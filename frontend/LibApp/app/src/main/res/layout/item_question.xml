<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

    <!-- Вопрос -->
    <TextView
            android:id="@+id/tvQuestion"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Какой год начался в 1805?"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:fontFamily="@font/montserrat" />

    <!-- ScrollView для прокручиваемого содержимого -->
    <android.widget.ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/tvQuestion"
            app:layout_constraintBottom_toBottomOf="parent"
            android:fillViewport="true">

    <!-- Варианты ответа -->
    <LinearLayout
            android:id="@+id/answerOptionsLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="8dp"
            android:paddingEnd="8dp">

        <!-- Первый вариант ответа с радиокнопкой -->
        <RadioButton
                android:id="@+id/answerOption1"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="A) июль 1805 года"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:padding="8dp" />

        <!-- Второй вариант ответа с радиокнопкой -->
        <RadioButton
                android:id="@+id/answerOption2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="B) июнь 1803 года"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:padding="8dp"
                android:fontFamily="@font/montserrat" />

        <!-- Третий вариант ответа с радиокнопкой -->
        <RadioButton
                android:id="@+id/answerOption3"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="C) август 1806 года"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:padding="8dp"
                android:fontFamily="@font/montserrat" />

        <!-- Четвертый вариант ответа с радиокнопкой -->
        <RadioButton
                android:id="@+id/answerOption4"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="D) сентябрь 1806 года"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:padding="8dp"
                android:fontFamily="@font/montserrat" />
    </LinearLayout>
</android.widget.ScrollView>

        </androidx.constraintlayout.widget.ConstraintLayout>
