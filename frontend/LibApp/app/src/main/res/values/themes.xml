<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LibApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Отключаем автоматический "фиолетовый" стиль для кнопок -->
        <item name="materialButtonStyle">@style/Widget.AppCompat.Button</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="Theme.LibApp" parent="Base.Theme.LibApp" />
</resources>